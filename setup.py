from setuptools import setup, find_packages

setup(
    name='custom_logic',
    version='0.0.1',
    description='Custom logic for ERPNext naming and workflows',
    author='Dein Name',
    author_email='dein.email@example.com',
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True,
    install_requires=[],
)
